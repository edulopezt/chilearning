# Módulo sence

Motor SENCE (RCE): asistencia e-learning vía Clave Única. SAGRADO: aislado, sin dependencias hacia otros módulos; tests contra el mock local.

Lógica de dominio pura (sin IO) en `domain/`.

> El contrato congelado del motor (tarea 0.5) reemplazará este archivo.
> Protocolo: `docs/sence/SPEC_INTEGRACION_SENCE.md`.
